mixin forgotForm()
  form(action="/login" method="POST")
    .form-group
      h4(style="margin-top: 30px;") Forgot password?
      label(for="email") Email Address
      input.form-control(type="email" name="email" placeholder="Currently disabled..." disabled)
    input.btn.btn-default(type="button" value="Reset Password" disabled)